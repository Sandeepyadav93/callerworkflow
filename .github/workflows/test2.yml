# This is a basic workflow to help you get started with Actions

name: CI

on:
  pull_request:
    branches: [ "main" ]  

jobs:
  call-workflow-from-test2:
    uses: Sandeepyadav93/test2/.github/workflows/test2.yml@main
    with:
      username: sandyy
